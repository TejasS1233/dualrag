services:
  neo4j:
    image: neo4j:latest
    container_name: dualrag-neo4j
    ports:
      - "7687:7687"
      - "7474:7474"
    environment:
      NEO4J_AUTH: neo4j/dualrag_password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
